# Third-party notices

Vendored dependencies live under `core/third_party/` and carry their own
licenses (tree-sitter and grammar submodules, usearch, xxHash,
onnxruntime-extensions). See each subtree for upstream license text.

No other third-party source was copied into `core/src/graph/` or the daemon
graph tools; that code is original to Codeberg.
