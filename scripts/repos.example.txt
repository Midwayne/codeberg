# One repo per line. Blank lines and lines beginning with # are ignored.
#
# Repo-name entries are cloned from --origin/CBERG_REPOS_ORIGIN if missing:
#   scripts/sync-repos.sh -c scripts/repos.example.txt -d ~/src --origin git@github.com:my-org
#
# Existing repos are pulled from origin with --ff-only.

codeberg

# You can also provide an explicit clone URL per repo:
# other-repo https://github.com/example/other-repo.git
