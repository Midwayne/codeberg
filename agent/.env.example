# Copy to agent/.env and adjust. Loaded by `make run-agent`.
# This file is the agent's config only — the daemon does not read it and never
# needs the LLM API key.

# Model the agent uses, as provider:model (required by `make run-agent`).
CODEBERG_MODEL=anthropic:claude-haiku-4-5

# API key for the provider named in CODEBERG_MODEL — set the matching one:
# ANTHROPIC_API_KEY=
# OPENAI_API_KEY=
# GOOGLE_GENERATIVE_AI_API_KEY=

# Daemon the agent queries over HTTP (default: http://127.0.0.1:8080)
# CODEBERG_DAEMON_URL=http://127.0.0.1:8080
